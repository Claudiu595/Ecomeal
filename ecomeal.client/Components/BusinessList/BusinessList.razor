@using EcoMeal.Site.Models;
@using EcoMeal.Site.Components.BusinessCard;
@namespace EcoMeal.Site.Components.BusinessList

@if (Businesses is null)
{
    <div class="text-muted py-4">Loading ...</div>
}
else if (Businesses.Count == 0)
{
    <div class="text-muted py-4">No businesses found.</div>
}
else
{
    <div class="row g-4">
        @foreach (BusinessModel business in Businesses)
        {
            <div class="col-12 col-md-6 col-xl-4">
                <BusinessCard Business="business"
                              OnDeleteRequested="HandleDelete" />
            </div>
        }
    </div>
}